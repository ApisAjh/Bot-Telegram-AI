{
  "version": 2,
  "builds": [
    {
      "src": "api/webhook.py",
      "use": "@vercel/python",
      "config": { "includeFiles": ["services/**"] }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "api/webhook.py" }
  ]
}
